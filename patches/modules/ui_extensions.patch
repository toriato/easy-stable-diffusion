127c127,128
<     tmpdir = os.path.join(paths.script_path, "tmp", dirname)
---
>     import tempfile
>     tmpdir = os.path.join(tempfile.gettempdir(), dirname)
135c136,139
<         os.rename(tmpdir, target_dir)
---
>         if os.stat(tmpdir).st_dev == os.stat(target_dir).st_dev:
>             os.rename(tmpdir, target_dir)
>         else:
>             shutil.copytree(tmpdir, target_dir)
